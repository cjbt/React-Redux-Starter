import React from 'react';
import { Main, Button } from '../MainContainer';

const MainContainer = () => {
  return (
    <div className='app'>
      <Main />
      <Button />
    </div>
  );
};

export default MainContainer;
